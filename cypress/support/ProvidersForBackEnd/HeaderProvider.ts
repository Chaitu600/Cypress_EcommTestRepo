// here we are basically doing encapsulating the bodyprovider property to header provider were code resuability is happening and no time involving to develope same thing againa and angain

// all the bodyProvide property can be used to headerProviders too

import BodyProvider from "./BodyProvider";

export default class HeaderProvider extends BodyProvider {}

